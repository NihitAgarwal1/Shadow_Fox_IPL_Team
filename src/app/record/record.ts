import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-record',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './record.html',
  styleUrl: './record.css',
})
export class Record {

}
